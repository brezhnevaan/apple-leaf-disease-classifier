max_epochs: 15
accelerator: auto
devices: auto
precision: 32
log_every_n_steps: 20

optimizer:
  name: adamw
  lr: 1e-3
  weight_decay: 1e-4

threshold: 0.5

scheduler:
  name: reduce_on_plateau
  monitor: val/loss
  mode: min
  factor: 0.1
  patience: 3
  min_lr: 1e-6
